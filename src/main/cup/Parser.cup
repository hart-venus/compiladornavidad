parser code {:
  // Any Java code to be added to the parser class should go here.
:}

terminal PLUS, MINUS, NUMBER;
non terminal S;

precedence left PLUS, MINUS;

S ::= S PLUS S
    | S MINUS S
    | NUMBER;
