function int main(){ 
    local int a <= 2|
    local int b| 
    local string some|
    local string body <= "y Adios mundo"|


    print(some)|

    some <= "Hola mundo "|
    print(some, body, " once told me ", a, 0.6)|
    a <= 3|
    print(a, b)|
}